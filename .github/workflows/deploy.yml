# yaml
name: Deploy static site to S3

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    # expose BUILD_DIR and S3_BUCKET as job env variables (can be overridden via repo secrets)
    env:
      BUILD_DIR: frontend/dist/
      S3_FE_BUCKET: ${{ secrets.AWS_S3_FE_BUCKET }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build
        working-directory: ./frontend
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5.1.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync to S3
        run: |
          # adjust BUILD_DIR if your build output is in a subfolder like dist/my-app
          aws s3 sync "$BUILD_DIR" "s3://$S3_FE_BUCKET" --delete --acl public-read
